void printList(Node*head){
Node*curr=head;
while(curr!=NULL){
cout<<(curr->data)<<"";
curr=curr->next;
}
}
Struct Node{
int data;
Node*next; // self referential structures
Node(int x) // constructo
{
data=x;
next=NULL;
}
}
int main(){
Node*head = newNode(10);
head->next= ""(20);
""= ""(30);
printList(head);
return 0;
}
